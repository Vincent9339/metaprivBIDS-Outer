# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - requirements: requirements.txt

sphinx:
  configuration: docs/conf.py  # Update this to point to your conf.py file